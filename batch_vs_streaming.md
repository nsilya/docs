Отличный вопрос. Статья «Command-Line Tools Can Be 235x Faster Than Your Hadoop Cluster» наглядно демонстрирует **важнейший принцип инженерии данных**: *выбирай инструмент по задаче — а не наоборот*. Вот чёткие критерии, когда использовать **Hadoop (или другие распределённые системы, типа Spark, Flink, EMR)** — и когда **локальную обработку через команды shell / Python / SQL**.

---

## ✅ **Используй localhost / CLI / одноузловые инструменты, если:**

1. **Объём данных ≤ 10–100 ГБ**  
   — Укладывается в RAM или быстро читается с диска.  
   — Пример: логи Nginx за год, датасеты NYC Taxi, CSV-выгрузки из CRM.

2. **Задача — потоковая, линейная, stateless**  
   — Фильтрация, агрегация, сортировка, простой ETL.  
   — Пример: посчитать топ-10 URL из логов, извлечь статистику из PGN-файлов.

3. **Нет необходимости в отказоустойчивости или масштабировании**  
   — Это разовый или периодический скрипт, а не production-сервис.

4. **Нужна скорость разработки и низкая стоимость**  
   — Нет кластера, нет DevOps-накладных расходов.  
   — Можно запустить на ноутбуке и получить результат за секунды.

5. **Память не требует хранения всего датасета**  
   — Обработка потоковая: читаешь строку → обрабатываешь → забываешь.  
   — Поэтому `awk`, `grep`, `cut`, `jq` идеальны.

> 🔥 **Правило большого пальца**: если задачу можно решить `cat | grep | sort | uniq | head` — **не трогай Hadoop**.

---

## ✅ **Используй Hadoop / Spark / распределённые системы, если:**

1. **Объём данных ≥ 100 ГБ — 1+ ТБ**  
   — Не помещается на одну машину (ни по RAM, ни по диску, ни по времени обработки).  
   — Пример: логи мобильного приложения за год от 10 млн пользователей.

2. **Требуется сложная обработка с зависимостями**  
   — Joins между большими таблицами, оконные функции, ML-модели на полном датасете.  
   — Пример: daily user behavior pipeline с feature engineering.

3. **Нужна устойчивость к сбоям (fault tolerance)**  
   — Задача критична, должна завершиться даже при падении ноды.

4. **Есть регулярная перезапускаемая инфраструктура**  
   — Уже есть кластер, Airflow, мониторинг, CI/CD для data pipelines.

5. **Данные распределены по разным источникам / зонам**  
   — HDFS, S3, Kafka → обработка "рядом с данными" экономит сетевой I/O.

> ⚠️ **Важно**: Hadoop/Spark не "быстрее" — они **масштабируемее**. Но с огромными накладными расходами: сериализация, shuffle, планирование, JVM-старт, сетевой overhead.

---

## 🔍 Применимо к тебе, Илья:

Ты — **Data Engineer с фокусом на batch-обработку, миграции, Airflow**.  
Твои задачи — **до 10–50 ГБ**, локальные или в облаке, но **без экстремального масштаба**.

**Твоя зона — localhost + CLI + Spark в Docker + Airflow.**  
Тебе **не нужен Hadoop**. Но тебе **крайне полезно уметь**:

- Писать эффективные `awk`/`jq`/`csvkit` скрипты — для быстрой проверки данных.  
- Запускать Spark локально — для отработки Iceberg/Delta-логики.  
- Оркестрировать это через Airflow — как production-grade pipeline.

> 💡 **Итог**:  
> **Hadoop — когда данных так много, что даже 64 ГБ RAM и NVMe SSD не спасают.**  
> **Во всех остальных случаях — начинай с localhost и CLI.**  
> А если задача решается за <5 минут на ноутбуке — **распределённые системы — это роскошь, а не необходимость**.

**Ты — инженер, а не админ кластера. Делай то, что приносит результат.**
