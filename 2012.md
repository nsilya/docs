SQL (оконные функции, CTE, JOIN-логика) — уже решаешь, продолжай.
Python: только то, что нужно для Airflow и валидации данных (pandas, hashlib, boto3).
Iceberg/S3/Parquet — только на уровне: «я знаю, как организовать partitioning, schema evolution, time travel».

Собери 3 конкретных кейса (даже если они учебные — оформи как реальные):
«Перенос клиентской БД из Oracle → S3 + Iceberg с валидацией целостности (hash-сверка, row count, дельта-анализ)»
«Миграция монолитного DWH на Airflow + Spark: выделение справочников → факты, dependency graph, обработка ошибок»
«Оркестрация API-миграции (как в хакатоне ВТБ): от анализа контрактов до запуска в проде»
Каждый кейс — в формате: Проблема → Технологии → Твоя роль → Результат (метрики: время, объём, % точности).
